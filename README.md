<h1 align="center">Desafio Front-end para <strong>Cielo</strong></h1>

# Front-end

## 1

Execute o comando `npm install` para instalar as dependencias.

## 2

Execute o comando `ng serve` para executar a aplicação.

# Backend

## 1

navegue até [/back-end], execute o comando `npm install` para instalar as dependencias

## 2

execute o comando `npm start` para executar o back-end
